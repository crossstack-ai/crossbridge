# ============================================================================
# CrossBridge AI - Requirements
# ============================================================================
# Product: CrossBridge by CrossStack AI (v0.2.0)
# Description: AI-powered test automation modernization platform
# Python Version: >=3.9, <3.15 (tested on 3.9-3.14)
# Last Updated: January 31, 2026
# ============================================================================

# ============================================================================
# CORE DEPENDENCIES (Required)
# ============================================================================

# Data Science & ML
numpy>=1.21.0,<2.0.0              # Array operations, numerical computing
scikit-learn>=1.0.0,<2.0.0        # Machine learning (flaky detection)

# System Monitoring (for sidecar observer)
psutil>=5.9.0,<7.0.0              # CPU and memory monitoring

# Database & ORM
SQLAlchemy>=2.0.0,<3.0.0          # Database ORM (upgraded to 2.x for better async support)
psycopg2-binary>=2.9.0,<3.0.0     # PostgreSQL adapter
pgvector>=0.2.0,<1.0.0            # PostgreSQL vector extension (embeddings)

# Data Validation
pydantic>=2.0.0,<3.0.0            # Data validation and settings management

# Configuration & Environment
python-dotenv>=1.0.0              # Environment variable management
PyYAML>=6.0,<7.0                  # YAML configuration parsing

# HTTP & Networking
requests>=2.31.0                  # HTTP client library
urllib3>=1.26.0,<3.0.0            # HTTP client (used by connectors)
certifi>=2023.0.0                 # SSL certificate bundle
aiohttp>=3.9.0,<4.0.0             # Async HTTP client (for Slack notifications)

# ============================================================================
# CLI & USER INTERFACE (Required)
# ============================================================================

typer>=0.9.0,<1.0.0               # Modern CLI framework
rich>=13.0.0,<14.0.0              # Beautiful terminal output
click>=8.0.0,<9.0.0               # Command-line interface (typer dependency)
tabulate>=0.9.0                   # Table formatting for console output

# Interactive prompts (currently not actively used, but planned)
# questionary>=2.0.0              # Uncomment when interactive features added

# ============================================================================
# API & OPENAPI TOOLS (For API Change Intelligence)
# ============================================================================

# Note: oasdiff is a Go binary, install separately:
#   go install github.com/tufin/oasdiff@latest
# Or download from: https://github.com/tufin/oasdiff/releases

# ============================================================================
# AI & LLM PROVIDERS (Optional - for AI-powered features)
# ============================================================================

openai>=1.0.0,<2.0.0              # OpenAI GPT models and embeddings
anthropic>=0.7.0,<1.0.0           # Anthropic Claude models

# Local & Open Source AI (Optional)
# ollama>=0.1.0                   # Uncomment for local Ollama embeddings
# sentence-transformers>=2.2.0    # Uncomment for HuggingFace embeddings

# ============================================================================
# VECTOR STORES & EMBEDDINGS (Optional - for semantic search)
# ============================================================================

faiss-cpu>=1.7.4,<2.0.0           # FAISS vector similarity search (CPU)
# faiss-gpu>=1.7.4                # Uncomment for GPU support (CUDA required)

# ============================================================================
# TEST AUTOMATION FRAMEWORKS (Optional - for translation)
# ============================================================================

playwright>=1.40.0                # Modern browser automation (Chromium/Firefox/WebKit)

# Note: Framework-specific dependencies installed as needed:
# - selenium>=4.0.0              # For Selenium translation/analysis (Java, Python, .NET)
# - robotframework>=6.0          # For Robot Framework support (adapters/robot/)
# - robotframework-requests>=0.9 # For Robot + RequestsLibrary API testing
# - robotframework-seleniumlibrary>=6.0  # For Robot + Selenium UI testing
# - cypress (via npm)            # npm install cypress
# - pytest plugins               # pytest-bdd, pytest-selenium, etc.
# - dotnet SDK 6.0+              # For Selenium .NET adapter (adapters/selenium_dotnet/)

# ============================================================================
# SOURCE CONTROL & REPOSITORY INTEGRATION (Optional)
# ============================================================================

PyGithub>=2.0.0                   # GitHub API integration
python-gitlab>=4.0.0              # GitLab API integration
atlassian-python-api>=3.41.0      # Bitbucket Cloud/Server API
azure-devops>=7.0.0               # Azure DevOps / TFS integration
msrest>=0.7.0                     # Azure authentication support

# ============================================================================
# DEVELOPMENT & TESTING (Dev Only)
# ============================================================================

pytest>=9.0.0                     # Testing framework (latest)
pytest-cov>=4.0.0                 # Coverage plugin
pytest-asyncio>=0.23.0            # Async test support
pytest-mock>=3.12.0               # Mocking support

# Code Quality
black>=23.0.0                     # Code formatter
flake8>=6.0.0                     # Linting
mypy>=1.0.0                       # Type checking
isort>=5.12.0                     # Import sorting

# ============================================================================
# PARSING & AST ANALYSIS (For intelligence features)
# ============================================================================

javalang>=0.13.0                  # Java AST parsing (for Java Step Parser)
# Note: Python AST uses built-in ast module
# Note: XML parsing uses built-in xml.etree.ElementTree
# Note: JSON parsing uses built-in json module
# Note: .NET parsing uses regex (no external C# parser)

# ============================================================================
# OPTIONAL STORAGE BACKENDS
# ============================================================================

# InfluxDB (time-series profiling data)
# influxdb-client>=1.36.0         # Uncomment for InfluxDB support

# Redis (caching and queuing)
# redis>=4.5.0                    # Uncomment for Redis support

# S3/MinIO (artifact storage)
# boto3>=1.26.0                   # Uncomment for AWS S3 support
# minio>=7.1.0                    # Uncomment for MinIO support

# ============================================================================
# TEMPLATE ENGINES & REPORTING (Optional)
# ============================================================================

# Jinja2>=3.1.0                   # Template engine (for prompt rendering)
# reportlab>=4.0.0                # PDF generation
# matplotlib>=3.7.0               # Charts and visualizations

# ============================================================================
# COMPATIBILITY NOTES
# ============================================================================
# 
# Python Version Support:
# - Minimum: Python 3.9 (EOL: October 2025)
# - Recommended: Python 3.11+ (better performance)
# - Tested: Python 3.9, 3.10, 3.11, 3.12, 3.13, 3.14
# - Maximum: Python 3.14 (current)
#
# SQLAlchemy 2.x Migration:
# - Upgraded from 1.4.x to 2.x for better async support
# - Breaking changes handled in persistence/schema.py
# - Uses new declarative_base and query syntax
#
# External Dependencies (Not in requirements.txt):
# - PostgreSQL 12+ (database server)
# - Grafana 9.0+ (visualization dashboards)
# - Node.js 16+ (for Cypress tests)
# - Java 11+ (for Java adapter support)
# - .NET 6.0+ (for SpecFlow adapter support)
#
# ============================================================================

# ============================================================================
# INSTALLATION
# ============================================================================
#
# Standard Installation (core features only):
#   pip install -r requirements.txt
#
# Development Installation (includes dev tools):
#   pip install -r requirements.txt
#   pip install black flake8 mypy isort pytest-mock pytest-asyncio
#
# Full Installation (all optional features):
#   pip install -r requirements.txt
#   pip install selenium robotframework influxdb-client redis boto3
#
# Production Installation (pinned versions):
#   pip install -r requirements.txt --no-deps
#   # Review and pin specific versions for production
#
# ============================================================================
