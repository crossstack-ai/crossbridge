# ============================================================================
# CrossBridge AI - Local Development Environment
# ============================================================================
# This file shows common settings for local development
# Copy to .env.local for local overrides (already gitignored)
# ============================================================================

# Local PostgreSQL (Docker or local install)
CROSSBRIDGE_DB_HOST=localhost
CROSSBRIDGE_DB_PORT=5432
CROSSBRIDGE_DB_NAME=crossbridge_dev
CROSSBRIDGE_DB_USER=postgres
CROSSBRIDGE_DB_PASSWORD=admin

# Local Grafana
GRAFANA_URL=http://localhost:3000
GRAFANA_API_KEY=dev_api_key_here

# Disable AI features for faster local testing
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
CROSSBRIDGE_EMBEDDINGS_ENABLED=false

# Enable verbose logging for debugging
CROSSBRIDGE_LOG_LEVEL=DEBUG

# Disable profiling for faster test runs
CROSSBRIDGE_PROFILING_ENABLED=false

# Local development environment
CROSSBRIDGE_ENVIRONMENT=development
CROSSBRIDGE_PRODUCT_NAME=CrossBridge-Dev
